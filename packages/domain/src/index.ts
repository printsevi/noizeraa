// Domain entities, value objects, and domain events live here.
// No framework imports (Nest, Drizzle, Express) — see CLAUDE.md and
// tech proposal §3.2. Repositories in @noizera/infra map rows to these
// types at the repository edge; this package never imports from infra.
export {};
